1. No, both VS and VSCode are text editors with their own strengths. VSCode is cross platform whilst VS is windows only. /
2. In theory yes, however each have their own use cases, .NET Framework is used for stability, whilst .NET5+ is used for added functionality. /
3. .NET Standard - is a formal specification that defines a set of APIs common across different .NET implementations, allowing developers to create libraries that can be used on any platform supporting that standard. /
4. .NET is a Common Runtime and not a programming language. X - I should add that F# and Visual Basic have their own compilers to Intermediate Language and .NET is a virtual machine that uses the IL to execute machine instructions.
5. Top-level program is an entry point without Public static void main string args boilerplate, `dotnet --help`. /
6. app.cs?
```C#
dotnet run app.cs
```

7. `<main>$` X - top level programs are defined with null namespace.
8. Microsoft learn /
9. Stack Overflow / GitHub /
10. Actually understand what it did by investigating and testing the code. /